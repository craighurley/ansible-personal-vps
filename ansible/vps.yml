---
- hosts: all
  sudo: yes
  gather_facts: yes
  vars_files:
    - "./private/vars/{{ ansible_os_family }}.yml"

  pre_tasks:

  roles:
    - users
    - ssh
    #- yum-update
    - yum
    - selinux
    - ntp
    - locale

  tasks:

  post_tasks:
